get all Depedencies

get productModel

reciveAllProducts(ordenation:string)
    try
        model.getAllProducts()
    catch e {
        trows ErrorCode(DatabaseERROR) 
        print(ErrorCode - Inside ProductControle reciveAllProducts)
    }

reciveProductByPagination(ordenation:string,page: int){
    try

        model.getProducstByPage()
    catch e {
        trows ErrorCode( DatabaseERROR)
        print ErroCode)
    }
    }

reciveProductHystory(ProductId:Int)
 try 
    model.getProductHistory()
catch e {
        trowErroCode (DatabaseERROR)
        printErroCode
}
}

